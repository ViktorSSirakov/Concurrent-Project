Finished Tasks:

Implemented data loading from CSV files.

Converted raw data points into initial single-point clusters.

Implemented cluster centroid calculation.

Implemented cluster merging functionality.

Implemented a CF-Tree structure for grouping input clusters.

Created Voronoi cells using centroids from the CF-Tree nodes.

Stored each Voronoi cell’s splitting point.

Implemented Voronoi-based cluster assignment.

Added overlap support using distance threshold d.

Implemented threaded splitting of clusters across Voronoi cells.

Added worker threads to divide the cluster list into chunks.

Added thread-safe insertion using per-cell logs.

Used mutexes to protect concurrent writes to Voronoi cell logs.

Added commit logic to move logged clusters into final Voronoi cells after threading.

Added summary output for Voronoi cell contents.

Implemented a dendrogram structure for hierarchical clustering.

Implemented closest-pair search between active clusters.

Implemented HAC cluster merging.

Stored merge history during HAC.

Implemented local HAC inside each Voronoi cell.

Added stopping condition for local HAC using threshold d.

Collected resulting local clusters after the Voronoi phase.

Implemented global HAC over all collected local clusters.

Added summary and history printing for local and global HAC.

Added command-line input for dataset file path.

Added command-line input for number of threads.

Added Makefile build support.



Tasks to be achieved:

Adding Paralelism for the CF-structure and Voronoi assigment.

BUilding a Queue keeping track of closest elements to each Active Cluster for faster lookup.

Adding Paralelism in the closest distance computation.

Adding multiple phases of Voronoi cells for deduction of the number of clusters.

Questions:

I have in the provied code 2 variables - MaxR which decides the maximum radious for the elements for nodes the CF structure 
and a constant d, which is the overlapping part in the POP. I be incrementing them for further steps of Voronoi, but I don't know
which starting values to put. Is there relevant research on the topic for general data?

I have currently being consedering the Centroids of the clusters. Should I work with other metrics (Mean for example)?

Note:

The report and the code is not currently ready but will be done and improved by the deadline!

GitHub Link: https://github.com/ViktorSSirakov/Concurrent-Project.git